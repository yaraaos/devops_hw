# Terraform інфраструктура AWS — lesson-5

Цей проєкт демонструє використання Terraform для створення інфраструктури в AWS за допомогою підходу Infrastructure as Code (IaC).

Інфраструктура включає:
- збереження Terraform state у S3 з блокуванням через DynamoDB
- мережеву інфраструктуру (VPC) з публічними та приватними підмережами
- ECR для зберігання Docker-образів

---

## 📁 Структура проєкту

lesson-5/
- main.tf — підключення модулів
- backend.tf — S3 бекенд для Terraform state
- outputs.tf — загальні outputs
- modules/
  - s3-backend/ — S3 + DynamoDB для state
  - vpc/ — мережева інфраструктура
  - ecr/ — ECR репозиторій
- README.md — документація

---

## ⚙️ Основні команди Terraform

Ініціалізація проєкту:
```bash
terraform init
Перевірка запланованих змін:

terraform plan
Створення ресурсів в AWS:

terraform apply
Видалення всієї інфраструктури:

terraform destroy
Команди apply та destroy можуть створювати або видаляти ресурси в AWS.
```

## 🧩 Опис модулів

---

### 📦 Модуль `s3-backend`

**Призначення:**  
Забезпечує централізоване та безпечне зберігання Terraform state-файлів.

**Створює ресурси:**
- S3-бакет для збереження `terraform.tfstate`
- версіювання обʼєктів у S3
- DynamoDB-таблицю для блокування state

**Навіщо використовується:**
- запобігання конфліктам при одночасній роботі з Terraform
- збереження історії змін state-файлу
- безпечна командна робота

---

### 🌐 Модуль `vpc`

**Призначення:**  
Створює базову мережеву інфраструктуру в AWS.

**Створює ресурси:**
- VPC з заданим CIDR-блоком
- 3 публічні підмережі
- 3 приватні підмережі
- Internet Gateway для публічних підмереж
- NAT Gateway для приватних підмереж
- Route Tables та маршрутизацію трафіку

**Використовується для:**
- ізоляції ресурсів
- контролю доступу до інтернету
- побудови безпечної мережі

---

### 🐳 Модуль `ecr`

**Призначення:**  
Надає сховище для Docker-образів.

**Створює ресурси:**
- ECR-репозиторій
- автоматичне сканування образів при push

**Результат:**
- отримання URL ECR-репозиторію
- готовність до CI/CD інтеграції
